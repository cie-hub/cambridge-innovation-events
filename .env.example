# MongoDB connection string
# Sign up for a free cluster at https://www.mongodb.com/atlas
# Database name: cambridge-events
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/cambridge-events

# Optional: protects /api/scrape from unauthorised access
# Set this in Vercel Environment Variables and the cron will send it as a Bearer token
CRON_SECRET=
